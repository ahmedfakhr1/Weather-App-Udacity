{
  "name": "weatherapp",
  "version": "1.0.0",
  "description": "## Overview in this project i created an asynchronous web app that uses Web API to fetch weather data from the web and obtain user data and combine those and store on my server data-structure,also dynamically updating the UI with the latest update on the server side.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
